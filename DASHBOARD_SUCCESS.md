# ✅ Дашборд успешно запущен!

## 🌐 Доступ к дашборду

Откройте в браузере:

**http://127.0.0.1:5000**

или

**http://localhost:5000**

## 📊 Что доступно

### Главная страница
- Статистика по сигналам
- Статус бота
- Последние сигналы
- Конфигурация
- Статистика кэша

### API Endpoints

Все API доступны по адресу `http://127.0.0.1:5000/api/`:

- `/api/statistics` - Общая статистика
- `/api/signals?limit=20` - Список сигналов
- `/api/bot/status` - Статус бота
- `/api/config` - Конфигурация
- `/api/cache/stats` - Статистика кэша

## 🔄 Автообновление

Дашборд автоматически обновляет данные каждые **30 секунд**.

Для ручного обновления нажмите кнопку **"🔄 Обновить"** в интерфейсе.

## 🛑 Остановка дашборда

Нажмите `Ctrl+C` в терминале, где запущен дашборд.

Или используйте скрипт:
```bash
./kill_dashboard.sh
```

## 📱 Доступ из сети

По умолчанию дашборд доступен только локально (`127.0.0.1`).

Для доступа из сети запустите:
```bash
DASHBOARD_HOST=0.0.0.0 ./start_dashboard.sh
```

**⚠️ Внимание**: Убедитесь, что настроен firewall!

## 🚀 Запуск в фоне

```bash
nohup ./start_dashboard.sh > dashboard.log 2>&1 &
```

## 📋 Проверка работы

1. Откройте http://127.0.0.1:5000 в браузере
2. Убедитесь, что видны карточки со статистикой
3. Проверьте таблицу последних сигналов
4. Статус бота должен показывать "🟢 Работает"

---

**Готово! Дашборд работает! 🎉**
















