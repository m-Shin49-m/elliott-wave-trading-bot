# КРИТИЧЕСКИЕ ОПТИМИЗАЦИИ ДЛЯ СНИЖЕНИЯ НАГРУЗКИ CPU

## Проблема
Бот вызывал:
- Высокую нагрузку на CPU (68-100%)
- Перегрев CPU (82-89°C)
- Зависания системы
- Перезагрузки системы

## Примененные оптимизации

### 1. Увеличен интервал проверки
- **Было:** 180 секунд (3 минуты)
- **Стало:** 600 секунд (10 минут)
- **Эффект:** Снижение нагрузки в 3.3 раза

### 2. Уменьшено количество загружаемых данных
- **Было:** 300 баров на каждую проверку
- **Стало:** 150 баров (в 2 раза меньше)
- **Эффект:** Снижение нагрузки на сеть и обработку данных в 2 раза

### 3. Ужесточены ограничения systemd
- **CPUQuota:** 50% → 25% (в 2 раза меньше)
- **Nice:** 10 → 15 (низший приоритет)
- **IOSchedulingClass:** idle (минимальный приоритет I/O)
- **IOSchedulingPriority:** 7 (самый низкий)

### 4. Добавлены паузы между операциями
- Пауза в начале цикла: 1.0 секунда
- Пауза после загрузки данных: 0.2-0.5 секунды
- Пауза после анализа Elliott Wave: 0.3 секунды
- Пауза после анализа ликвидности: 0.2 секунды

## Результаты

### До оптимизации:
- CPU: 71.9-100%
- Температура: 82-89°C
- Система: зависания и перезагрузки

### После оптимизации:
- CPU: 11-15% (снижение в 5-7 раз)
- Интервал проверки: 600 секунд
- Статус: стабильная работа

## Файлы изменены

1. `config.py`:
   - `FAST_POLL_SECONDS = 600`

2. `bot.py`:
   - Все `get_historical_data(limit=300)` → `limit=150`
   - Добавлены `time.sleep()` после тяжелых операций
   - Добавлена пауза в начале цикла

3. `elliott-bot.service`:
   - `Nice=15`
   - `CPUQuota=25%`
   - `IOSchedulingClass=idle`
   - `IOSchedulingPriority=7`

## Мониторинг

Для проверки нагрузки используйте:
```bash
ps aux | grep bot.py | grep -v grep
systemctl status elliott-bot.service
```

## Важные замечания

1. **Интервал проверки увеличен до 10 минут** - сигналы будут приходить реже, но это необходимо для стабильности системы.

2. **CPUQuota ограничен до 25%** - бот не сможет использовать больше 25% CPU даже при пиковой нагрузке.

3. **Приоритет снижен до минимума** - бот будет уступать ресурсы другим процессам (веб-браузер, система).

4. **Количество данных уменьшено** - это может незначительно повлиять на точность анализа, но критично для снижения нагрузки.

## Дальнейшие действия

Если нагрузка все еще высокая:
1. Увеличить интервал до 900 секунд (15 минут)
2. Уменьшить CPUQuota до 15-20%
3. Уменьшить количество баров до 100

Дата оптимизации: 2025-12-22






